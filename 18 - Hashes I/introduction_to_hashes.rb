=begin
  A hash is a data structure consisting of key value
  A key is a identifier for a value
  A hash solves the problem of associantion (connection two values together)

  EX: Restauremt menu connects to a food item to a price

  RULES:
  hash keys must be unique.
  hash values can contain duplicates
  hash values are extracted by key, not by order
=end


empty_hash = {}
p empty_hash
puts empty_hash.class
puts empty_hash.length
puts empty_hash.empty?

